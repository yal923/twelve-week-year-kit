# WorkBuddy 已有测试补证｜只读，不重跑

用途：完成两端[能力对照](../../reviews/capability-comparison-2026-09-20.md)所需的最小证据。已收到 shareable-summary；目前无需重新执行试验。下方文字可交给原 WorkBuddy 会话，文件本身不替代账号持有人指令。

## 可直接交给 WorkBuddy 的文字

请只读取本次测试已经保存的日志、工具轨迹和本地证据，生成一个新的 `shareable-evidence-addendum.md`，放在本次私有运行目录，并告诉我完整路径。不修改已有摘要，不重新调用 Notion，不创建／更新／删除任何测试对象，不安装或切换连接。

请完成以下五项：

1. **环境与接口。** 补充 WorkBuddy 版本（可从已有记录或已安装应用的只读版本信息取得），实际 Notion 工具／连接名称及可见版本，数字字段相关的工具输入类型定义。无法取得的保持 unknown；不要读取凭证或私有连接配置。注明各信息是本次运行时已保存，还是事后从当前应用读取。
2. **一成功、一失败的数字请求。** 从已有记录各选一次，保留真实工具名、参数结构、Week／Order／ProbeValue 的原始 JSON 类型、返回状态与错误 message。脱敏时只能替换身份信息；保留数字是否有引号，不将字符串转换成数字。若记录中有更下游实际请求体，可附对应片段并标来源；没有则说明“未观察到下游请求”，不要凭错误反推完整请求或断言是哪一层转换。
3. **尝试表。** 对摘要所称的 8 次尝试逐条列出：尝试序号、原日志事件相对顺序、create／update 或其他动作、逻辑目标（虚构目标／W1A／W1B／W2C）、字段及类型、结果、有无返回对象／异步任务、后续回读、再次派发的依据。说明“8 次”包含几次成功和失败，如何得到最终四记录及零次成功修改；若先建缺少数字的记录再补写、超出原计划调整过动作或另获范围批准，如实列出既有记录，没有则标未记录。不新增任何授权。
4. **最后实际状态。** 仅从已经保存的回读中摘出三条虚构行动的 Name／Week／Order／ProbeValue／Double、保存的视图筛选／排序／显示列及实际结果行集；空值保持 null 或注明未返回，不补成预期值。列出分页的实际 has_more／是否有 next_cursor，说明有没有真正取过下一页；说明 description 是从请求、机器回读还是界面观察核对，以及标记核对受限后是否暂停。无对应记录的写缺证据，不联网补测。
5. **重试结论。** 附实际观察到的异步状态片段与关联动作（若只有文档列出 retrying 而未实际出现，也请区分）。说明数字错误判为“未执行且无副作用”的具体依据，区分工具级拒绝与多步骤调用中某一步失败。仅看到 retrying 或 HTTP 400 不标为安全保证；没有政策／控制证据则保留 unverified。说明已有有限例外的范围和是否另有批准，勿附私人的聊天原文。

所有真实账号、页面、工作区、实例／记录／任务 ID 及 URL 用稳定代号替换，例如 ROOT_A、DB_A、ROW_W1B、TASK_A；同一个对象始终使用同一代号，映射只留本机。删除姓名、邮箱、本机绝对路径、凭证、签名链接和无关内容。只摘取本次虚构试验相关片段，不打包整个工作目录或聊天记录。

明确每段是“既有工具回执的脱敏摘录”“日志条目”还是“说明／推断”。没有保留的原始请求或回执就写未保留，不能用模型重新构造一份 JSON 冒充原始证据。报告没有证据的位置本身就是有用结果，不要求为了填满表格重新测试。

## 返回位置与后续处理

由账号持有人检查后把新增补充文件交回，与之前摘要放在同一私有接收目录。开发侧先核对缺口，再定义必要的补测；本清单不批准新一轮写入，不要求上传全部原始日志。旧摘要和已发测试 ZIP 均保留原样。
