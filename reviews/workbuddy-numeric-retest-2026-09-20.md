# WorkBuddy 数字复测核对｜NUM-01.1

日期：2026-09-20。对应 R14–R16／R23、A16–A18／A25。仅核对交回材料，规格与实现授权不变。

## 结论与证据边界

**接收侧报告：W1A.ProbeValue 数字 2→5 的单次更新成功，并已独立读回。本轮采纳为该次局部功能成功的报告，不视为原失败场景已修复或全产品兼容通过。**

- 来源：`numeric-diagnostic-summary.md`，运行时间声明为 2026-09-20 11:53–12:02（GMT+8），计划 [NUM-01.1](../docs/superpowers/plans/2026-09-20-workbuddy-number-check.md) 位于提交 `ac1d578da8f6d46f35ae7593f055c626d465b268`。
- 收件 SHA-256：`5ae0b9f1e8219d9369d32cf5a4128170891454803854deaf98d81d97482866b4`。原件保留私有且不改写；原摘要及补证也保持不变。
- 交回内容是脱敏摘要、模型侧请求摘录和结果陈述。按计划，完整回执、映射、日志和本人确认留在接收方，本轮没有取得或独立验证这些原件，也未访问该设备／账号。摘要所称“直接落盘并重读”保留为接收侧报告，不能当成本轮完成了原始证据审计。
- 当前已安装 WorkBuddy 5.5.6 来自该侧会话事后读取应用信息，进程未自报版本；工具／连接版本 unknown。macOS 26.6.2、arm64 为该侧报告实测。

## 本次观察

| 项目 | 交回材料 | 本轮判读 |
|---|---|---|
| 前态 | ProbeValue=2、Week=1、Order=10；父链与稳定键匹配 | 支持预期前态；完整核对来源见下节保留项 |
| 主动数字更新 | 1 次，成功 1、拒绝 0、未知 0；仅 ProbeValue=5，JSON number | 与单字段、一次更新范围相符；实际完整轨迹未交回 |
| 写响应 | 返回页面代号，无错误，无 async_task | 支持接收侧报告本次同步成功；内部是否重试仍未知 |
| 独立读回 | 1 次；ProbeValue 为 number 5，其余受检字段、关系和父位置未变 | 支持该次数字更新局部成功；报告称 5 保留，未还原 |
| 其他能力 | Double 仍为不可解析引用；无异步查询、无新建、无重试 | 公式、数字创建、失败恢复均未通过本次验证 |

请求摘录与 NUM-01.1 固定请求的字段、数字类型及 allow_async 值一致。前置读取报告 6 次，写后读取 1 次，异步查询 0 次；这些是摘要计数，不能冒充已逐次对账的工具轨迹。

## 与上次失败的关系

原补证 #4 已报告 W1A 的 Week／Order／ProbeValue 三项数字补写成功；失败发生在带数字的创建、W1B 的数字更新和 W2C 的数字更新。本次仍使用 W1A，只改其已有 ProbeValue。

因此，本次为一个已有成功记录的单字段更新增加了正面证据。它没有复现并修复原失败请求：不能推出“之前只是网络不稳定”“重试即可解决”“所有数字字段恢复正常”，也不能把前后合并成可代表可靠性的成功率。记录、字段组合、数值和时间不同，未控制这些差异；两次材料均未观察下游请求体，根因保持未知。

## 按计划执行：保留未验证项

接收侧自评 `pass（含一处偏差记录）`；本轮对**完整按计划执行**保留 `unverified`，不因此否定单次功能结果，也不在证据不足时直接判为超额或越权。

1. **404 后的父链与调用计数。**摘要称按数据库 ID 读取返回 404，随后通过 data source 完成核对，同时称六次前置读取全部完成。data source 响应及根页面内容可能提供所需父链，但摘要没有列出每次调用及它实际证明哪条关系。应从既有日志列出六次调用的序号、工具、目标代号、结果及父链证据位置；把 404 计入总数。当前既不能认定用了第七次调用，也不能仅凭“改通道成功”认定全部前置条件已经满足。
2. **旧请求的确定性。**“上次运行末条为无副作用 400”不是无在途／未知写入的充分依据，原补证末条还是 W2C。应说明与 W1A 有关的旧尝试分别如何结束，不借其他对象的最后一条记录作保证。撤回“400 必然无副作用”的推断，不补写历史回执或新的授权。
3. **重复 as-of 时间。**相同时间戳本身不能证明缓存或读到过期数据；写后新时间戳也不能证明前置读取是否新鲜。保留为待解释的观察，不定位为网络／缓存根因。若需确认，使用本次已存前态、临写前复查的时间及字段摘录，不新增在线调用来补造当时证据。

以上只需核对接收侧**已有**日志并提供必要脱敏摘录；材料未留存就标 unknown。无需交出完整私人日志，也不需要重新写一次数字。本人确认、实际请求直接落盘、派发前日志等其他声明同样尚未独立审计；报告范围与原单文件交付约定一致。

## 后续位置

- 本次数字摘要已收件，不再要求重跑 NUM-01.1；其前提 ProbeValue=2 已被本次结果改变，不能原样复用或先自动恢复为 2。
- 固定 Notion 页面／字段／关系／视图的工程定义可继续细化；保留真实 number 类型和写后核对，不根据本次结果更换连接或修改产品方法。
- 原失败分支、标记／公式机器回读、分页、异常接续等仍需后续明确范围的验证。若要正式关闭“按计划执行”，先完成上述已有日志的只读核对；任何新写入需另有具体计划与该账号授权。
- U01／U07、A17／A25 与完整产品兼容仍开放。本轮仅更新证据判断和交接状态，没有派发新的 Notion 调用、恢复测试值或开始产品实现。
